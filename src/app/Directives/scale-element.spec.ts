import { ScaleElement } from './scale-element';

describe('ScaleElement', () => {
  it('should create an instance', () => {
    const directive = new ScaleElement();
    expect(directive).toBeTruthy();
  });
});
